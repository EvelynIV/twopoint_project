# 模块化任务型 Python 项目 Skill 范式总结

## 一、Skill 定位

该 skill 用于创建、扩展或重构具有以下特点的 Python 项目：

- 项目包含多个相互协作但应尽量解耦的模块。
- 项目通过命令行选择任务、配置和运行方式。
- 同一能力可能存在多个可替换实现，需要方便地对比验证。
- 项目需要单元测试、模块级诊断工具和整体任务测试。
- 项目重视配置复用、问题定位、可测试性和后续扩展。

该 skill 只规定工程范式、目录习惯、接口习惯和测试习惯，不包含任何具体业务流程、算法或硬件实现。

## 二、总体原则

1. 使用一个统一的 Typer 应用作为项目命令入口。
2. 将全局运行环境、任务参数和临时命令行覆盖分层管理。
3. 将任务定义与整体调度代码统一放在 `tasks/` 目录。
4. 将具体能力按职责拆成独立模块，避免跨模块访问内部实现。
5. 使用稳定接口和统一数据对象连接模块。
6. 将可能变化的实现封装在工厂、注册表或依赖注入接口之后。
7. 每个模块都应具备脱离完整系统单独测试和诊断的能力。
8. 自动化测试、集成测试和人工诊断工具应明确分层。
9. 所有需要用户设置的参数都应提供合理默认值。
10. 所有代码注释和文档字符串统一使用中文。
11. 实时任务应支持可选的状态绘制、实时转发和原始/绘制双路录像。

## 三、推荐目录结构

```text
project/
├── pyproject.toml
├── .env.example
├── configs/
│   └── tasks/
│       ├── task_a.json
│       └── task_b.json
├── src/
│   └── project_name/
│       ├── command/
│       │   └── app.py
│       ├── config/
│       │   ├── models.py
│       │   └── loader.py
│       ├── contracts/
│       │   ├── protocols.py
│       │   └── models.py
│       ├── tasks/
│       │   ├── registry.py
│       │   ├── task_a.py
│       │   └── task_b.py
│       └── modules/
│           ├── module_a/
│           └── module_b/
├── tests/
│   ├── unit/
│   ├── integration/
│   └── hardware/
└── tools/
    └── diagnostics/
```

目录名称应表达职责，不使用无语义的数字后缀。目录中的模块数量增加后，继续按领域拆分，不把不同职责堆积到单个大文件中。

## 四、命令行范式

### 4.1 统一使用 Typer

项目主入口、测试辅助命令、人工诊断命令和独立演示命令统一使用 Typer，不混用 argparse 等其他命令行框架。

主命令只负责：

1. 解析参数。
2. 加载环境变量和配置文件。
3. 构造经过校验的配置对象。
4. 调用 `tasks/` 中的任务入口。
5. 将异常转换成清晰消息和稳定退出码。

命令行层不得直接实现业务逻辑。

### 4.2 所有参数提供默认值

所有 Typer 参数和选项都应提供明确默认值，保证用户不传参数时也能查看帮助、执行安全检查或运行默认流程。

默认值应遵循以下规则：

- 优先使用安全、保守、可解释的默认值。
- 默认值集中定义，避免散落在多个命令或模块中。
- 路径默认值使用 `Path`。
- 布尔选项使用清晰的正反开关语义。
- 时间、次数、并发数和阈值等数值必须同时定义合法范围。
- 会产生外部副作用的命令，默认采用只读、预览或不执行危险动作的模式。
- 无法安全提供真实值时，使用禁用状态或示例值，并在运行前进行明确校验。

配置优先级统一为：

```text
命令行显式参数 > 环境变量 > 任务 JSON > 代码默认值
```

### 4.3 注册为正式命令

在 `pyproject.toml` 中注册 console script，使项目安装后可以直接执行统一命令，不依赖手动拼接模块路径。

## 五、配置范式

### 5.1 配置分层

- `.env`：保存与部署环境、运行机器或全局资源有关的参数。
- `configs/tasks/*.json`：保存单个任务需要的参数。
- Typer 参数：用于一次性覆盖和调试。
- 代码默认值：保证配置缺省时行为确定。

环境变量不得重复承载任务内部的细节参数，任务 JSON 也不得保存只属于某台机器的环境信息。

### 5.2 强类型配置

配置进入任务代码前必须转换成强类型、不可变或受控可变的配置对象，并完成：

- 类型校验。
- 必填项校验。
- 数值范围校验。
- 枚举值校验。
- 路径存在性或可访问性校验。
- 未知字段检查。
- 跨字段约束检查。

禁止在业务代码中反复读取原始字典或直接调用 `os.getenv()`。

### 5.3 可追踪性

程序启动时应输出最终生效的配置来源和配置摘要。敏感字段必须脱敏，不能写入日志。

仓库应提供 `.env.example` 和任务 JSON 示例，但不得提交真实密钥、个人路径或仅适用于单台机器的私有信息。

## 六、任务与整体调度范式

### 6.1 统一放入 `tasks/`

以下代码必须位于 `tasks/` 目录：

- 任务入口。
- 多模块组合与调用顺序。
- 整体状态流转。
- 生命周期管理。
- 启动、停止和异常清理。
- 任务级重试、超时与退出条件。

能力模块只实现自己的职责，不负责决定完整任务如何运行。

### 6.2 控制任务文件规模

任务文件应以编排为主，不应同时实现底层协议、算法、数据转换、记录输出和界面展示。

当一个任务文件出现以下情况时应继续拆分：

- 同时包含三个以上明显不同的职责。
- 大量私有函数只服务于某个独立子流程。
- 测试必须构造过多无关依赖。
- 修改一个功能经常影响其他流程。
- 文件已经难以在一次阅读中理解完整结构。

可按需要拆分为调度器、状态机、控制循环、输出适配器和资源管理器，但拆分名称应根据项目实际职责确定。

### 6.3 使用注册表分发

任务选择采用显式注册表，不通过持续增长的 `if/elif` 链分发：

```text
任务名称 → 配置模型 → 任务执行函数
```

注册时检查名称重复、配置类型不匹配和任务入口签名错误。新增任务时，只增加任务模块、注册项、配置示例和对应测试。

## 七、模块与接口范式

### 7.1 按职责拆分

每个模块只处理一种明确能力，并通过公开接口提供服务。模块之间不得依赖对方的私有函数、内部状态或具体实现类。

公共配置、接口和数据结构放在 `contracts/` 或等价的公共层中。依赖方向应保持：

```text
命令入口 → 任务编排 → 公共契约 ← 具体能力模块
```

公共契约层不得反向导入具体实现。

### 7.2 固定模块接口

使用 `Protocol`、抽象基类或明确的函数签名描述最小接口。接口应定义：

- 输入类型。
- 输出类型。
- 异常语义。
- 超时语义。
- 生命周期方法。
- 是否允许阻塞。
- 是否具有外部副作用。

模块间传递结构化数据对象，不使用含义不清的多层字典或位置元组。

### 7.3 支持多实现替换

对可能出现多种方案的关键能力：

- 将实现隐藏在稳定接口之后。
- 使用工厂函数或注册表选择实现。
- 让选择依据来自配置，而不是散落在业务代码中的条件判断。
- 为每种实现执行同一组契约测试。
- 保证替换实现时不需要修改任务编排的核心逻辑。

## 八、实时预览、绘制与录像范式

### 8.1 使用独立输出分支

实时数据进入输出阶段后，至少保留以下逻辑分支：

```text
最新原始帧
├── 原始录像分支
└── 帧副本 → 状态绘制
              ├── 绘制版录像分支
              └── WebRTC 实时转发分支
```

绘制必须作用于原始帧的副本，禁止原地修改原始帧。原始录像必须保持未经绘制的内容，绘制版录像和实时转发使用同一份绘制结果，避免不同输出中的状态表现不一致。

### 8.2 WebRTC 实时查看

项目应支持启动 WebRTC 服务实时查看绘制后的运行状态，并遵守：

- WebRTC 服务由任务生命周期统一启动和停止。
- 转发线程或异步服务不得阻塞采集、处理和任务调度。
- 转发只消费最新可用帧，不积压历史帧。
- 客户端速度不足时允许丢弃旧帧，优先保证低延迟。
- 客户端连接、断开或网络异常不得导致主任务退出。
- 服务地址、端口和启用状态由环境变量配置，并具有安全默认值。
- 服务停止时必须关闭连接、后台任务和网络资源。

### 8.3 使用环境变量控制可选能力

绘制和实时转发必须能够分别启用或关闭，不得绑定为同一个开关。建议至少定义以下通用配置：

```dotenv
APP_DRAW_ENABLED=true
APP_WEBRTC_ENABLED=false
APP_WEBRTC_HOST=0.0.0.0
APP_WEBRTC_PORT=8080
APP_RECORD_RAW_ENABLED=true
APP_RECORD_ANNOTATED_ENABLED=true
```

实际项目可替换统一前缀，但同一项目内必须保持一致。布尔环境变量应严格识别 `true/false`、`1/0`、`yes/no` 和 `on/off`，遇到无法识别的值应明确报错，不能依赖 `bool("false")` 等错误转换。

组合行为必须明确：

- 关闭绘制时，不执行绘制计算；实时转发可回退为原始画面。
- 关闭实时转发时，不启动 WebRTC 服务，但录像能力保持独立可用。
- 原始录像和绘制版录像使用独立开关。
- 启用绘制版录像时必须生成绘制分支，不能污染原始录像。
- 所有开关的最终生效值应在启动日志中输出。

### 8.4 同步保存原始版和绘制版

启用双路录像时，应同步保存未经绘制和带绘制的两版视频：

- 两个输出使用相同的输入帧 ID、尺寸、帧率和时间基准。
- 同一输入帧要么同时写入两路，要么记录明确的丢帧原因。
- 两个文件共享同一个会话时间戳，通过后缀区分 `raw` 和 `annotated`。
- 写入失败时应指出失败分支，不能静默影响另一分支。
- 任务退出或异常时必须释放两个编码器并完成文件收尾。

推荐命名格式：

```text
{task_name}_{MMDD_HHMMSS}_raw.mp4
{task_name}_{MMDD_HHMMSS}_annotated.mp4
```

时间戳至少包含月、日、时，并建议同时包含分、秒以避免同一小时内重名。时间戳必须在录像会话创建时生成一次，两路文件不得分别取时间。

### 8.5 绘制样式保持克制

绘制的目标是辅助观察状态，不应大面积遮挡原始画面：

- 线条默认使用细线，在常见分辨率下优先采用 1～2 像素。
- 点的默认半径优先采用 2～4 像素。
- 字体、描边和标签背景保持紧凑，不遮挡关键区域。
- 线宽、点半径和字号应按画面短边成比例缩放，并设置合理上下限。
- 颜色应具有足够对比度，但避免使用大面积实心填充。
- 多种状态使用稳定、统一的颜色语义。
- 绘制样式集中在一个配置对象或主题中，不在各模块散落魔法数字。

### 8.6 保持实时链路解耦

绘制、编码和转发属于输出适配能力，不得进入核心处理模块。任务编排只负责创建并连接输出适配器。输出适配器通过统一接口接收带帧 ID 和时间戳的数据对象。

当绘制、录像或转发速度低于主链路时，应使用有界缓冲、覆盖式最新状态或明确的丢帧策略，不能让输出能力拖慢核心任务。需要严格双路同步时，只在录像适配器内部协调两路写入，不改变主链路的实时语义。

## 九、测试模块编写范式

### 9.1 测试分层

- `tests/unit/`：测试纯逻辑、配置、数据转换和单个类或函数。
- `tests/integration/`：测试多个模块的组合和接口衔接。
- `tests/hardware/`：测试依赖特定设备、系统服务或外部环境的能力。
- `tools/diagnostics/`：保存需要人工执行和观察结果的诊断命令。

默认测试命令只运行不依赖外部环境的单元测试。集成测试和硬件测试必须通过独立命令、标记或环境开关显式启用。

### 9.2 测试命令统一使用 Typer

能够作为脚本独立运行的测试、诊断或演示文件，其命令行入口必须使用 Typer。所有选项提供默认值，并支持通过 `--help` 查看用途、参数和默认行为。

Typer 只负责测试命令的参数收集和结果展示，测试逻辑应封装为可直接调用的函数，便于自动化测试复用。

### 9.3 文件与方法注释规范

每个测试文件开头必须使用中文三引号文档字符串说明：

- 文件测试的对象。
- 主要覆盖范围。
- 是否依赖外部环境。
- 如何独立运行。

示例形式：

```python
"""
验证某模块的公开接口、异常处理和边界条件。

该文件默认不依赖外部环境，可通过统一测试命令运行。
"""
```

每个测试方法、辅助方法和可独立调用的方法都必须包含中文文档字符串，说明该方法验证或执行的内容。

代码中的行内注释和块注释统一使用中文。注释应解释设计意图、边界条件和不明显的原因，不重复描述代码表面行为。

### 9.4 使用 Fake、Mock 和依赖注入

任务代码应允许注入外部依赖，使测试可以替换真实资源。测试替身应遵守与真实实现相同的公开接口。

优先测试：

- 正常路径。
- 默认参数路径。
- 配置覆盖优先级。
- 空输入和边界值。
- 超时与失败恢复。
- 资源释放。
- 重复启动和停止。
- 替换不同实现后的契约一致性。
- 绘制与转发环境变量的各种开关组合。
- 原始帧不会被绘制分支修改。
- 双路录像的帧 ID、时间戳、尺寸和帧率保持一致。
- 成对录像文件共享同一时间戳并具有正确后缀。
- WebRTC 客户端异常不会影响主任务。
- 绘制线宽和点半径符合样式范围。

### 9.5 测试数据保持集中

重复配置使用 fixture、builder 或工厂函数创建。测试只覆盖与当前断言有关的参数，避免复制大段配置导致测试和示例文件发生漂移。

## 十、文档与注释习惯

- Python 文件使用模块级中文文档字符串说明职责。
- 类和公开函数使用中文文档字符串说明用途、参数、返回值和异常。
- 每个方法都必须具有中文文档字符串，包括测试方法和辅助方法。
- 注释统一使用中文。
- 名称表达职责，不依赖注释弥补模糊命名。
- 文档描述接口和使用方式，不复制具体实现细节。
- 复杂流程优先拆分和命名，其次再补充注释。

## 十一、质量与交付检查

完成新增或重构后，至少检查：

1. 是否只有一个正式 Typer 主入口。
2. 独立测试和诊断命令是否也使用 Typer。
3. 所有可设置参数是否具有安全默认值。
4. 配置优先级是否一致且经过强类型校验。
5. 任务和整体调度是否位于 `tasks/`。
6. 具体模块是否只承担单一职责。
7. 公共契约是否没有反向依赖具体实现。
8. 可替换实现是否共享同一接口和契约测试。
9. 测试文件是否包含中文模块文档字符串。
10. 每个方法是否包含中文文档字符串。
11. 所有注释是否统一使用中文。
12. 单元测试、集成测试和外部环境测试是否隔离。
13. 错误路径、超时和资源释放是否有测试。
14. README、示例配置和命令帮助是否保持一致。
15. 是否排除了缓存、构建结果、日志和临时输出。
16. 绘制、WebRTC 转发和两路录像是否可以独立配置。
17. 原始帧是否始终不受绘制分支污染。
18. 原始版和绘制版录像是否使用同一会话时间戳。
19. 录像文件名是否包含月日时分秒和明确版本后缀。
20. 绘制使用的线、点和文字是否避免遮挡画面。
21. 慢客户端或慢编码器是否不会阻塞核心任务。

## 十二、后续 Skill 建议内容

正式制作 skill 时，建议只包含：

```text
modular-task-python/
├── SKILL.md
├── agents/
│   └── openai.yaml
├── references/
│   ├── project-layout.md
│   ├── configuration-rules.md
│   ├── testing-rules.md
│   └── visualization-recording-rules.md
├── scripts/
│   └── check_project_conventions.py
└── assets/
    └── project-template/
```

- `SKILL.md`：保存核心创建流程和必须遵守的规则。
- `references/`：保存目录、配置和测试的详细规范。
- `scripts/`：检查 Typer 使用、默认值、目录位置、中文文档字符串和测试分层。
- `assets/`：提供不含具体业务逻辑的最小项目骨架。

正式 skill 应保持简洁，并使用渐进式披露：核心规则放在 `SKILL.md`，详细模板和检查项按需读取。任何模板都只能表达工程结构，不得预置具体业务实现。
